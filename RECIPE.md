# AI Build Recipe — Vibe Starter Market 판매 랜딩

> 검증 단계 산출물. 이 폴더(`web/`)는 plan.md 6절 풀 하네스를 **면제**받고 이 `RECIPE.md`만 필수다.
> (적용 시점: [`../docs/harness/overview.md`](../docs/harness/overview.md) §1)
> 목적: 비개발자·개발자 모두에게 보여줄 **판매 랜딩 1개**를 AI로 안 무너지게 만든다(plan.md 9-2).

## 0. 시작 전 (Setup)

* AI 도구: <Claude Code / Cursor 등>. 에이전트에게 먼저 [`AGENTS.md`](AGENTS.md)를 읽힌다.
* 스택: Next.js + TS + Tailwind (plan.md 7-3: php 제외, Next.js로 통일).
* 범위: **랜딩 1페이지.** 결제는 호스팅형 위젯 또는 사전예약/문의 폼만(plan.md 7-1, 포트 추상화 금지).

## 1. 합의 (Plan)

```
AGENTS.md를 먼저 읽어. 단일 랜딩 페이지를 만들 거야.
목표: 스타터 패키지(하네스+레시피)를 파는 랜딩.
범위: 1페이지(히어로 / 가치제안 / 포함내용 / 아키텍처 다이어그램 / AGENTS.md 발췌 / 구매 버튼 mock).
제약: 결제 실연동 없음(사전예약·문의 폼). 백엔드 없음.
코드 전에 섹션 구성과 컴포넌트 목록을 먼저 보여줘.
```

* 사람이 결정: 메시지 카피, 가격 표시 방식, CTA(사전예약 vs 문의).

## 2. 뼈대 (Scaffold)

> 구조 → 화면 → 기능. "한번에 다" 금지.

1. 구조: `섹션 컴포넌트 골격만 만들어(Hero, Value, Included, Architecture, AgentsExcerpt, CTA). 내용은 비워둬.`
2. 화면: `Hero와 Value 섹션만 채워. 색/간격은 하드코딩 말고 Tailwind 토큰/테마로.`
3. 기능: `CTA 버튼에 사전예약/문의 폼만 연결(mock). 실제 결제 연동은 하지 마.`

## 3. 복구 (Recover)

```
멈춰. 지금 무엇이 보이고 무엇이 깨졌는지 요약해.
마지막으로 렌더된 섹션으로 좁혀서 하나씩 고쳐. 페이지를 통째로 재생성하지 마.
```

## 4. 확장 (Extend)

```
새 섹션 1개만 기존 컴포넌트 패턴대로 추가해. 기존 섹션 구조는 건드리지 마. 계획 먼저.
```

## Do / Don't (대화 원칙)

* **Do:** 한번에 하나씩 · 변경 전 계획 요청 · 결정(카피·가격·CTA)은 사람이.
* **Don't:** "알아서 랜딩 다 만들어줘" · 결제 백엔드/포트 선구축(검증 전 과설계) · 통째 재생성.
